namespace LivinOnSweets.API.Enums
{
    // i named it this way because it should represent the master difficulties, each resource pack contains
    // a variety of difficulties for each song and in my perspective, each pack should implement its own custom song screen
    public enum SweetSongDifficulty
    {
        Easy,
        Normal,
        Hard
    }
}
